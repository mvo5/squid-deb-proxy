#!/bin/sh -e
# A simplified version of debconf's own config script.
. /usr/share/debconf/confmodule
# Ask for the rsyslog host
db_input low squid-deb-proxy/ppa-enable || true
db_input low squid-deb-proxy/acl-enable || true
db_go
